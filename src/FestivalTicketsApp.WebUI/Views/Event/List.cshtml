@model FestivalTicketsApp.WebUI.Models.Event.EventListViewModel

<div class="container">
    @{
        await Html.RenderPartialAsync("FilterPart");
        await Html.RenderPartialAsync("EventsPart", Model.Events);
    }
</div>