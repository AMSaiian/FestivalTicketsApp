@model FestivalTicketsApp.WebUI.Models.Host.HostListViewModel
<div class="container h-100 d-flex flex-column">
    @{
        await Html.RenderPartialAsync("FilterPart");
        await Html.RenderPartialAsync("HostsPart", Model.Hosts);
        await Html.RenderPartialAsync("Pagination", new PaginationModel(Model.CurrentPageNum, Model.NextPagesAmount));
    }
</div>